<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="CTAppTheme">
        <attr name="text_base" format="color" />
        <attr name="base_background" format="color" />
        <attr name="divider_gray" format="color" />

        <!-- Detailed chart view -->
        <attr name="tick_label_gray" format="color" />
        <attr name="shadow_gray" format="color" />

        <!-- CompoundChartView -->
        <attr name="text_blue" format="color" />

        <!-- CustomSeekBar -->
        <attr name="seek_bar_fill_gray" format="color" />
        <attr name="seek_bar_stroke_blue" format="color" />
    </declare-styleable>
</resources>